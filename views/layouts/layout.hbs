<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body style="background-color: {{details.bgc}};">
    <div class="navbar">
        <a class="norm" href="/">Linktree</a>
        {{#if auth}}
            <a class="abnorm" href="/users/{{auth}}">Profile</a>
            <a class="abnorm" id="logout" href="/logout">Logout</a>
        {{else}}
            <a class="abnorm" href="/login">Sign In</a>
        {{/if}}
    </div>
    
    {{{ body }}}
</body>
</html>